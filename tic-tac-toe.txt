Tic Tac Toe - Notes on Installation and Coding
-------------------------------------
Installation
-------------------------------------

To start this project, I had to first figure out how to install Ubuntu as a way for me to clone the repository onto my computer and push changes automatically. Since I had my virtualization options disabled for my Windows laptop, I first had to tamper with my control panel and change my DNScache settings in my BIOS files so that I could install Ubuntu, which required multiple reboots due to constant troubleshooting. As it turns out, a lot of my DNS-related settings were disabled aspart of an old method I used to block ads across websites.

Once I downloaded Ubuntu, I used sudo commands to locally download Git and Node.js into my computer so that I can use React commands and clone the repository of this assignment. Using this clone, I could locally push my own changes without intering with the original repository.

-------------------------------------
Coding: Tic-Tac-Toe
-------------------------------------

For this assignment, I started off by following a tutorial on how to build a simple tic-tac-toe game using React. All code is written in the /src/App.js file.

Began by defining a default function called Square, which returns a displayable JSX button from the square CSS class

export default function Square() {
  return <button className="square">X</button><button className="square">X</button>;
}

Since React components can only return a single JSX element per line, used <> and </> fragments to wrap around multiple objects, counting them as one to return:

  return (
    <>
      <button className="square">X</button>
      <button className="square">X</button>
      <button className="square">X</button>
      <button className="square">X</button>
      <button className="square">X</button>
      <button className="square">X</button>
      <button className="square">X</button>
      <button className="square">X</button>
      <button className="square">X</button>
    </>
  );

From there, used <div className="board-row"> tags to separate the squares into a grid instead of a straight line
Changed function name to "Board" instead of "Square" to better reflect design
Rendered the JS variable {value} in the Square function and used it to display an empty board to later be filled with values 1-9

Created handleClick function to make the board interactive, confirmed that the clicks were registering properly by having each click return a "clicked!" message in the console after attaching an onClick command to the button element

Imported the { useState } component from React library and replaced the {value} component with useState instead, so that it now returns value as a state
Removed value props from the Square components created by the Board component and set the initial value as a const with [value, setValue] as components equal to setState(null)

Replaced the "clicked!" console.log command to instead set the value to 'X' upon clicking on a square inside the board, causing an X to show up upon clicking
Board function now declares a state variable called squares that defaults to an array of 9 nulls that corresponds with the 9 squares on the board
Changed the board-row divs such that the value prop is passed down to each square that is clicked, so that the program registers that an X is now present on the board instead of a null empty space

Removed the original onClick prop entirely from the Square component so that it can instead receive the {value} prop like before
Added new onClick prop that calls a new function onSquareClick whenever a square is clicked on
Connected the onSquareClick prop to the handleClick function in Board by passing the function to the Square component's onSquare prop

Implemented the following code into the handleClick in order to update the state of the board within the array hidden in the program so that it knows where an X has been placed on the board and what squares remain empty; the slice() method creates this hidden copy of the array for the sript to reference

const nextSquares = squares.slice();
    nextSquares[0] = "X";
    setSquares(nextSquares);

Added parameter i component to the handleClick function so that any square can be clicked and updated to display an X instead of just the top left square
Updated onSquareClick component to be able to pass and receive the updated i parameter from handleClick when the function is called, by using the arrow function () => handleClick(#) for each of the nine squares on the board using numbers 0-8 as array index values

Updated Board and handleClick functions to operate with the boolean state variable [xIsNext, setXIsNext] in order to flip between placing an 'X' or 'O' value upon clicking a square and therefore moving to the next player's turn, which can be done by flipping xIsNext from true to false after every click, and vice versa
Updated handleClick to return early if a square is already occupied so that clicking on one square that already displays a value will not overwrite that value with the other

Created the calculateWinner function in order to p roperly end the game and decide on a winner by analyzing the copied array, which is done by checking the array and comparing its current values to manually written square lists of possible winning combinations that correspond to the indexes of the array
The calculateWinner function uses a for loop to constantly check for a winner after every click and will return null if there is no winner, otherwise it will return whatever the value of the winning square list is on the array, being X or O

Updated handleClick function to call calculateWinner every time a click occurs to check for a winner, in the same line as checking if a square is occupied or not
Added a status prompt to display a string message on who the winner is based on what value is returned by the calculateWinner function

Although the tic-tac-toe game is fully functional at this point, the tutorial provides more regarding adding an option to return to previous moves

Created new top-level component function called Game that is now the new export default function above Board that contains the entire game history
Used divs and board-row classes in order to render the Board function and therefore the board itself within the Game function
Added the boolean [xIsNext, setXIsNext] and array [history, setHistory] useState variables to the Game function as well as a currentSquares component that has a value of history[history.length - 1] in order to read the previous squares arrays

Changed Board function to receive xIsNext, squares, and onPlay as props and removed all useState variables as they are now parameters built into the function
Inside handleClick function, added a call for onPlay so that the Game function updates when the user clicks a square

Created the handlePlay function inside the Game function that rerenders the copied array, but uses history as a state variables with setHistory([...history, nextSquares]) instead of the Board component's setSquares function, as well as having it toggle xIsNext inside the function
Added the array map method to transform the values recorded by the history variable into an array of React elements, including a moves variable that tracks all past squares arrays and a jumpTo function that allows the user to click a button to recreate the board using a map of the array at the time the button was mapped and created
Fixed the key error created by the program being unable to differentiate the array React elements from one another by adding key={move} inside the list tag

Created a new state variable called currentMove so that Game can track down what step of the game the user is currently viewing
Updated jumpTo function inside the Game function to include setXIsNext(nextMove % 2 === 0) to indicate that the player will be using X if the number of moves passed is even

Changed handlePlay function to remove all future array states if the user chose to go back in time to a previous move using const nextHistory = [...history.slice(0, currentMove + 1), nextSquares] so that only that portion of the history is kept, along with adding setCurrentMove(nextHistory.length - 1) to update currentMove to match the step number after going back in time
Fixed Game function such that the currentSquares component now renders the last move, even after a time travel has occurred, by setting it to  history[currentMove]

Changed the xIsNext component inside the Game function to const xIsNext = currentMove % 2 === 0 to avoid redundancy since this component will already know when the player is supposed to be using an X or an O, making any calls to setXIsNext unnecessary
